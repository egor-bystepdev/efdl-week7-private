# Репорт

## Setup (установка зависимостей)
```bash
uv sync --all-groups
```

## Test (с покрытием)
```bash
uv run pytest tests/ --cov=src --cov-report=term-missing -v
```

Покрытие 96%, правда пришлось бодаться с мокерами (чисто для %) и думается, это уже интеграционная часть (ибо мы буквально тестим что вызывается запуск сервиса). Мб в некст году понизить запрос на покрытие?
Я мог бы разнести запуск в отдельный файл и закрыть omit'ом, но хз

## Metrics

Кроме предложенной в README `app_http_inference_count` ничего более не накидывал (ибо не требовалось), хотя можно добавить Summary для времени токенайзера/модели например.

----- 
В остальном только grpc порт вместо 9090 использую 50051 ибо CI ожидает внутри контейнера 50051 (а менять CI чтобы было меньше вопросов я не хочу)

Генерейтед протобуфы оставил в коде (некоторые не любят такое и генерируют при сборке)

В докере сразу подгружаю модель чтобы первый запрос не был кратно дольше (или вообще отлетал по таймауту)